/** Border-radius scale (px) extracted from the "Worship App" mockup. */
export const radii = {
  xs: 5,
  sm: 6,
  smd: 7,
  md: 8,
  mdl: 9,
  lg: 10,
  lgl: 11,
  xl: 12,
  xxl: 14,
  /** For a fully circular element — pass `width / 2` (or half the element's size) rather than this constant directly. */
  circle: 9999,
} as const;
